'use client';
import{useState,useEffect}from'react';const mockDrivers=[{id:'1',firstName:'John',lastName:'Doe',email:'john@example.com',status:'pending',categories:['EXEC','LUX']},{id:'2',firstName:'Jane',lastName:'Smith',email:'jane@example.com',status:'active',categories:['EXEC']},{id:'3',firstName:'Mike',lastName:'Johnson',email:'mike@example.com',status:'active',categories:['SUV','VAN']},{id:'4',firstName:'Sarah',lastName:'Williams',email:'sarah@example.com',status:'pending',categories:['LUX']}];export function useOperatorDriversList(){const[drivers,setDrivers]=useState(mockDrivers);const[loading,setLoading]=useState(true);const stats={total:mockDrivers.length,pending:mockDrivers.filter(d=>d.status==='pending').length,active:mockDrivers.filter(d=>d.status==='active').length};useEffect(()=>{const fetch=async()=>{setLoading(true);await new Promise(r=>setTimeout(r,400));setLoading(false)};fetch()},[]);return{drivers,loading,stats}}
