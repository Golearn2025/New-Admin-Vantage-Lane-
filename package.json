{
  "name": "@vantage-lane/admin",
  "version": "0.1.0",
  "description": "Admin dashboard for Vantage Lane ride-hailing platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "check:ts": "tsc --noEmit",
    "check:lint": "eslint . --ext .ts,.tsx -f unix",
    "quality:check": "npm run guard:app-logic && npm run format:check && npm run lint && npm run check:ts",
    "quality:fix": "npm run format && npm run lint --fix",
    "check:next": "next build",
    "check:all": "mkdir -p reports && (tsc --noEmit 2>&1 | tee reports/tsc.log); (eslint . --ext .ts,.tsx -f unix 2>&1 | tee reports/eslint.log); (next build 2>&1 | tee reports/next-build.log)",
    "lh:login": "npx @lhci/cli autorun --collect.url=http://localhost:3000/login --collect.startServerCommand='npm run dev' --assert.assertions.categories:performance=0.9 --assert.assertions.categories:accessibility=0.95",
    "svgo:icons": "npx svgo --config=svgo.config.js --folder=apps/admin/shared/ui/icons/svg --output=apps/admin/shared/ui/icons/svg",
    "check:enterprise": "npm run check:boundaries && npm run check:files && npm run check:colors && npm run check:business",
    "check:boundaries": "npx depcruise -c .dependency-cruiser.cjs apps/admin --exclude '^node_modules' > reports/depcruise.txt || true",
    "check:circular": "npx madge apps/admin --circular > reports/circular.txt || true",
    "check:files": "find apps/admin -name '*.tsx' -not -path '*/node_modules/*' -exec wc -l {} \\; | awk '$1>200{print \"UI too long: \"$2\" (\"$1\" lines)\"}' || true",
    "check:colors": "grep -R 'color:\\s*#\\|rgb(\\|hsl(' apps/admin app --include='*.css' --include='*.tsx' -n || echo 'No inline colors found'",
    "check:business": "grep -R 'from.*shared/api/clients\\|fetch(\\|axios(\\|supabase\\.from(' app --include='*.tsx' --include='*.ts' -n || echo 'No business logic in UI'",
    "check:any": "grep -R '\\bany\\b' apps/admin/shared/ui app/login --include='*.ts' --include='*.tsx' -n || echo 'No any types found'",
    "check:budgets": "npx size-limit --why || true",
    "check:a11y": "npx @axe-core/cli http://localhost:3000/login -o reports/axe-login.json || true",
    "check:health": "bash scripts/check-health.sh",
    "check:p0": "npm run check:health",
    "check:everything": "bash scripts/check-everything.sh",
    "check:quality": "bash scripts/check-quality.sh",
    "check:duplicates": "bash scripts/check-duplicates.sh",
    "check:deadcode": "bash scripts/check-deadcode.sh",
    "check:performance": "bash scripts/check-performance.sh",
    "check:advanced": "npm run check:quality && npm run check:duplicates && npm run check:deadcode && npm run check:performance",
    "gen:component": "plop component",
    "guard:ui": "node scripts/guard-components.mjs",
    "guard:app-logic": "bash scripts/guard-app-logic.sh"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --max-warnings 0",
      "prettier --write"
    ],
    "*.{css,scss}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@supabase/ssr": "^0.7.0",
    "@supabase/supabase-js": "^2.75.0",
    "dotenv": "^17.2.3",
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "recharts": "^2.15.4",
    "swr": "^2.3.6",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@axe-core/cli": "^4.10.2",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.19.22",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "dependency-cruiser": "^17.0.2",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0",
    "eslint-plugin-import": "^2.29.0",
    "husky": "^8.0.0",
    "madge": "^8.0.0",
    "plop": "^4.0.4",
    "prettier": "3.2.5",
    "size-limit": "^11.2.0",
    "tsup": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
